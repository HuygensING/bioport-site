from zope.interface import Interface

class IBioport(Interface):
    "Marker interface for the Bioport grok app"

class IEnglishRequest(Interface):
    "Marker interface for requests in the english language"
